# Pinned dependencies for the plain virtualenv route (no Docker / devcontainer).
# Kept in sync with pyproject.toml and the devcontainer.
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
networkx==3.6.1
numpy==1.26.4
matplotlib==3.8.0
pytest==8.2.0
